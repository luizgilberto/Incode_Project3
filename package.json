{
  "name": "project3",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "create-database": "set PGPASSWORD=Senha123&& psql -U postgres -h localhost -d mrcoffee -f sql/create_database.sql",
    "create-schedules": "set PGPASSWORD=Senha123&& psql -U postgres -h localhost -d mrcoffee -f sql/create_schedules.sql"

  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.0.1",
    "ejs": "^3.1.6",
    "express": "^4.17.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.7"
  }
}
